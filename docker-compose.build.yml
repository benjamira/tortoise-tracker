# Optional override to build the images locally from source instead of pulling:
#   docker compose -f docker-compose.yml -f docker-compose.build.yml up -d --build
services:
  tortoise-tracker-api:
    build: ./backend
  tortoise-tracker-web:
    build: ./frontend
